import styled from 'styled-components';

const HealthCheck = () => {
  return <HealthCheckContainer>health check</HealthCheckContainer>;
};

const HealthCheckContainer = styled.div`
  font-size: 24px;
  color: #fff;
`;

export default HealthCheck;
